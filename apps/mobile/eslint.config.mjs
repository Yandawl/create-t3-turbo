import baseConfig from "@webmeric/eslint-config/base";
import reactConfig from "@webmeric/eslint-config/react";

/** @type {import('typescript-eslint').Config} */
export default [
  {
    ignores: [".expo/**", "expo-plugins/**"],
  },
  ...baseConfig,
  ...reactConfig,
];
